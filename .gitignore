# Binaries (root only, not cmd/ directories)
/arc-relay
/arc-sync
/mcp-wrangler
/mcp-sync
!/cmd/arc-relay/

# Database
*.db
*.db-journal
*.db-wal
*.db-shm

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment / local config
.env
.env.*
config.toml
.mcp.json

# Secrets & credentials
*.pem
*.key
*.p12
*.pfx
credentials.*
secrets.*

# Go
/vendor/

# Build artifacts
/dist/
*.exe
# Cross-compiled binaries (e.g. arc-sync-darwin-arm64)
arc-sync-*
arc-relay-*
# Nightshift plan artifacts (keep out of version control)
.nightshift-plan
.claude/worktrees/
.claude/scheduled_tasks.lock
